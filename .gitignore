# Internal working files, not part of the published site: the client brief,
# the sitemap, the revision notes and the supplied spec sheets.
/docs/

# OS cruft
Thumbs.db
.DS_Store

# Local browser-test harnesses, never part of the site
__*.html

# Client-supplied source material dropped in for processing, not site content
/Images to add/
